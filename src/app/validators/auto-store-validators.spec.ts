import { AutoStoreValidators } from './auto-store-validators';

describe('AutoStoreValidators', () => {
  it('should create an instance', () => {
    expect(new AutoStoreValidators()).toBeTruthy();
  });
});
